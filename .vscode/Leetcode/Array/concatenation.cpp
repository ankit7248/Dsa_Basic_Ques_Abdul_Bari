#include <bits/stdc++.h>
using namespace std;

int main()
{

    vector<int> ans;
    int nums;
    cin >> nums;

    for (int i = 0; i < nums; i++)
    {
        int n;
        cin >> n;
        ans.push_back(n);
    }

    for (int i = 0; i < nums; i++)
    {

        ans.push_back(n);
    }
    return ans;
}
