#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<string> s = {"codeleet"};

    vector<int> indices = {4, 5, 6, 7, 0, 2, 1, 3};
    vector<string> ans;

    sort(indices.begin(), indices.end());

    vector<int>::iterator it;
    for (it = indices.begin(); it != indices.end(); it++)
    {
        }
}