class Solution {
public:
    
      int arr(a[],int target)
      {
          for(i=0;i<nums-1;i++)
              for(j=i;j<nums-1;j++)
              {
                  sum =a[i]+a[i+1];
                  if(sum == target)
                   return a[i],a[j] ;
                      else
                          continue;
              }
      }
};

int main()
{
solution s
int nums; 
int a[nums];
s.arr(nums-1,target);
return 0;
}
